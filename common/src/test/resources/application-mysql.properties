## MySQL-backed test profile (schema persists in MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/store-java?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=p0o9i8u7
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Keep schema around for inspection
spring.jpa.hibernate.ddl-auto=update

# Ensure Spring Boot test slice doesn't auto-replace with embedded DB
spring.test.database.replace=none

spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE
logging.level.org.hibernate.tool.schema=DEBUG
